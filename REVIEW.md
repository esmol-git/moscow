# Ревью перед заливкой на сервер (показ заказчику)

**Дата:** 12.02.2026  
**Проект:** antalya-house.ru (Antalya House)

---

## ✅ Сделано в рамках ревью

1. **Год в футере** — обновлён на 2026.
2. **PHP в сборке** — папка `src/php` копируется в `dist/php` при `npm run build`, при деплое по FTP скрипт отправки форм попадает на сервер (путь `php/sendmail/index.php`).

---

## ⚠️ Обязательно перед показом

### Контакты (сейчас заглушки)

- **Телефон:** в футере и в блоке «Контакты» указан `+7 (495) 123-45-67` — заменить на реальный.
- **Email:** в блоке «Контакты» указан `info@example.com` — заменить на реальный.
- **Соцсети:** в футере ссылки ВКонтакте и Telegram ведут на `#` — подставить реальные URL.

Файлы для правки:
- `src/components/layout/footer/footer.html` — телефон, ВК, Telegram.
- `src/components/pages/index/index.html` — блок `#contacts`: телефон и email.

### Favicon

- В шапке подключён `@img/favicon.ico`, в проекте файла нет — иконка будет 404.
- **Действие:** положить `favicon.ico` в `src/assets/img/` или убрать/заменить ссылку в `src/components/layout/head/head.html`.

### Пароли и секреты на сервере

- **FTP:** пароль в `template.config.js` — для деплоя с другого места лучше использовать переменную окружения `FTP_PASSWORD`.
- **PHP sendmail:** в `src/php/sendmail/config.php` хранятся логин и пароль SMTP Mail.ru. В репозитории их лучше не держать: вынести в env на хостинге или в отдельный конфиг вне git. После заливки проверить, что письма уходят на нужный ящик (в `index.php` получатель — `esmolakov65@gmail.com`).

---

## ✅ Проверено, всё ок

| Раздел | Статус |
|--------|--------|
| **SEO** | На всех страницах (index, krasnodar, kaliningrad): title, description, keywords, canonical, Open Graph (og:url, og:type, og:title, og:description). |
| **Формы** | Формы с `data-fls-form="ajax"`, `action="php/sendmail/index.php"`. После отправки показывается попап «Заявка принята», форма очищается. |
| **PHP** | Валидация имени и телефона, JSON-ответ, `display_errors = 0`, ошибки в лог. |
| **Мобильная вёрстка** | Hero на малых экранах по высоте контента, блок «Смотреть дальше» скрыт до 768px. |
| **Сборка** | При `npm run build` в `dist` попадают HTML, assets и папка `php` (sendmail). |

---

## 📋 Рекомендации (по желанию)

- **og:image** — добавить в `head` мета-тег с картинкой для расшаривания в соцсетях (например, логотип или превью главной).
- **Консоль:** при сборке могут быть предупреждения `ReferenceError: 'canonical'` — они уже устранены на страницах krasnodar/kaliningrad; при появлении снова проверить шаблоны.
- **После заливки:** проверить отправку формы на боевом домене (часто нужен HTTPS и корректный путь к `php/sendmail/index.php`).

---

## Чек-лист перед деплоем

- [ ] Подставить реальные телефон, email, ВК, Telegram.
- [ ] Добавить favicon в `src/assets/img/` или поправить head.
- [ ] При необходимости сменить получателя писем в `src/php/sendmail/index.php` (addAddress).
- [ ] Выполнить `npm run build`.
- [ ] Убедиться, что в `dist` есть папка `dist/php/sendmail/` с index.php и config.php.
- [ ] Залить содержимое `dist` на сервер (FTP или `npm run deploy`).
- [ ] На хостинге проверить, что PHP включён и путь к скрипту совпадает с `action` форм.
- [ ] Отправить тестовую заявку и проверить доставку письма.
